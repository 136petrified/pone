#include <gtest/gtest.h>

#include "ponegate.hpp"
#include "ponetile.hpp"

/*

TEST(ponegate, Constructor) {
    Gate g1;
    GATE tilePair = g1.getGate();
    EXPECT_EQ(tilePair.first, nullptr);
    EXPECT_EQ(tilePair.second, nullptr);

    Tile t1 = Tile{2, 3, "none", "empty"};
    Tile t2 = Tile{4, 5, "none", "empty"};

    Gate g2{&t1, &t2};

    Tile *target1 = g2.getTile1();
    EXPECT_EQ(target1->getX(), 2);
    EXPECT_EQ(target1->getY(), 3);

    Tile *target2 = g2.getTile2();
    EXPECT_EQ(target2->getX(), 4);
    EXPECT_EQ(target2->getY(), 5);
}

TEST(ponegate, ID) {
    Gate g;
    g.setID(64);
    EXPECT_EQ(g.getID(), 64);
}

TEST(ponegate, Color) {
    Gate g;
    g.setColor("green");
    EXPECT_EQ(g.getColor(), "green");
}

TEST(ponegate, Name) {
    Gate g;
    g.setName("g1");
    EXPECT_EQ(g.getName(), "g1");
}

*/

// Uncomment later
