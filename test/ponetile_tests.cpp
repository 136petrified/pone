#include <gtest/gtest.h>

#include "ponetile.hpp"

/*
TEST(ponetile, Constructor) {
    Tile t;
    EXPECT_EQ(t.getX(), 0);
    EXPECT_EQ(t.getY(), 0);
    EXPECT_TRUE(t.isEmpty());
}

TEST(ponetile, X) {
    Tile t;
    t.setX(3);
    EXPECT_EQ(t.getX(), 3);

    t.setX(-2);
    EXPECT_EQ(t.getX(), -2);
}

TEST(ponetile, Y) {
    Tile t;
    t.setY(3);
    EXPECT_EQ(t.getY(), 3);

    t.setY(-2);
    EXPECT_EQ(t.getY(), -2);
}

TEST(ponetile, Color) {
    Tile t;
    t.setColor("red");
    EXPECT_TRUE(t.getColor() == "red");
}

TEST(ponetile, isCollision) {
    Tile collisionTile;
    collisionTile.setType("collision");
    EXPECT_TRUE(collisionTile.isCollision());
}

TEST(ponetile, isCursor) {
    Tile cursorTile;
    cursorTile.setCursor(true);
    EXPECT_TRUE(cursorTile.isCursor());
}

TEST(ponetile, isEmpty) {
    Tile newTile;
    EXPECT_TRUE(newTile.isEmpty());
}

TEST(ponetile, Equals) {
    Tile t1{1, -1, "none", "empty"};
    Tile t2{1, -1, "red", "collision"};
    EXPECT_TRUE(t1 == t2);
}

*/
